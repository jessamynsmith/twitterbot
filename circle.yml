---
machine:
    python:
        version: 2.7.9
        version: 3.4.2

dependencies:
    override:
        - pip install -r requirements/development.txt

test:
    override:
        - flake8
        - tox
        - PYTHONPATH=. coveralls

deployment:
  production:
    branch: master
    heroku:
      appname: heartbotapp
