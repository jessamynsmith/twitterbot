dependencies:
  cache_directories:
    - ".pythonz"
  pre:
    - bash ./.circleci/install-pythons.sh

test:
  override:
    - tox

deployment:
    production:
      branch: master
      heroku:
        appname: heartbotapp
