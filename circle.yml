---
dependencies:
    override:
        - pip install -r requirements/development.txt

test:
    override:
        - flake8
        - tox
        - PYTHONPATH=. coveralls

deployment:
  production:
    branch: master
    heroku:
      appname: heartbotapp
